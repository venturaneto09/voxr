---
// SPDX-License-Identifier: AGPL-3.0-or-later
import {Font} from 'astro:assets';

interface Props {
	readonly preloadFallback: boolean;
}

const {preloadFallback} = Astro.props;
---

<Font cssVariable="--font-radio-canada-big" preload />
<Font cssVariable="--font-jetbrains-mono" preload />
<Font cssVariable="--font-noto-sans" preload={preloadFallback} />
