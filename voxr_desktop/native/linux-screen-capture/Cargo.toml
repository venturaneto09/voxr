[package]
name = "voxr_linux_screen_capture"
version = "0.0.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false

[workspace]
resolver = "2"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
wgpu = ["dep:wgpu", "voxr_gpu_rebuild/wgpu"]

[dependencies]
napi = {version = "3.9.1", default-features = false, features = ["dyn-symbols", "napi8", "async"]}
napi-derive = "3.5.6"
parking_lot = "0.12"
voxr_screen_frame_bus = { path = "../screen-frame-bus" }
voxr_gpu_rebuild = { path = "../gpu-rebuild" }
wgpu = { version = "29", optional = true, default-features = false }

[target.'cfg(target_os = "linux")'.dependencies]
async-io = "2.6.0"
dcv-color-primitives = "1.0"
futures-lite = "2.6.1"
libc = "0.2"
pipewire = { version = "0.10.0", features = ["v0_3_33"] }
zbus = "5.16.0"

[dev-dependencies]
criterion = { version = "0.8", default-features = false, features = ["cargo_bench_support"] }

[build-dependencies]
napi-build = "2.3.2"

[[bench]]
name = "pipewire_callback"
harness = false
