[package]
name = "voxr_system_hunspell"
version = "0.0.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false

[workspace]
resolver = "2"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
voxr_desktop_native = {path = "../rust"}
hunspell-sys = {version = "0.3.1", features = ["bundled"]}
napi = {version = "3.9.1", default-features = false, features = ["dyn-symbols", "napi8"]}
napi-derive = "3.5.6"

[build-dependencies]
napi-build = "2.3.2"

[dev-dependencies]
tempfile = "3.27"
