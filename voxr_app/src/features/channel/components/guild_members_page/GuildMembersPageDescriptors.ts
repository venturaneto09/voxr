// SPDX-License-Identifier: AGPL-3.0-or-later

import {msg} from '@lingui/core/macro';

export const VANITY_URL_DESCRIPTOR = msg({
	message: 'Vanity URL',
	comment:
		'Join source label on the community members page. Shown when the member joined via the community vanity URL.',
});
export const ALL_DESCRIPTOR = msg({
	message: 'All',
	comment: 'Date-range filter option meaning no time window is applied. Used on the community members page.',
});
